# package.json pins node to >=22 <23, but npm treats engines as advisory and
# installs anyway with only a warning. The node on PATH outside fnm is v24, so
# any shell where fnm's --use-on-cd has not fired would otherwise build on the
# wrong runtime and fail somewhere far less obvious than the install.
engine-strict=true
